<?php

return [

    /*
    |--------------------------------------------------------------------------
    | فهرس الكلمات (document_search_tokens)
    |--------------------------------------------------------------------------
    | يستبدل LIKE على search_words باستعلامات مفهرسة — مناسب لـ cPanel و30k+ وثيقة.
    */
    'use_token_index' => env('DOCUMENT_SEARCH_USE_TOKEN_INDEX', true),

    /*
    |--------------------------------------------------------------------------
    | تخزين مؤقت لنطاق البحث (أعداد التبويبات + خطة التجميع)
    |--------------------------------------------------------------------------
    | يعمل مع cache driver الافتراضي (database/file) بدون Redis.
    */
    'scope_cache_minutes' => (int) env('DOCUMENT_SEARCH_SCOPE_CACHE_MINUTES', 30),

    /*
    |--------------------------------------------------------------------------
    | حد أقصى لوثائق خطة التجميع المتفرقة (حماية من استعلامات ضخمة)
    |--------------------------------------------------------------------------
    */
    'scattered_plan_max_docs' => (int) env('DOCUMENT_SEARCH_SCATTERED_MAX_DOCS', 15000),

];

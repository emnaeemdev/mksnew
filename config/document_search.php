<?php

return [

    /*
    |--------------------------------------------------------------------------
    | فهرس الكلمات (document_search_tokens)
    |--------------------------------------------------------------------------
    | يستبدل LIKE على search_words باستعلامات مفهرسة — مناسب لـ cPanel و30k+ وثيقة.
    */
    'use_token_index' => env('DOCUMENT_SEARCH_USE_TOKEN_INDEX', true),

    /*
    |--------------------------------------------------------------------------
    | تخزين مؤقت لنطاق البحث (أعداد التبويبات + خطة التجميع)
    |--------------------------------------------------------------------------
    | يعمل مع cache driver الافتراضي (database/file) بدون Redis.
    */
    'scope_cache_minutes' => (int) env('DOCUMENT_SEARCH_SCOPE_CACHE_MINUTES', 30),

    /*
    |--------------------------------------------------------------------------
    | تخزين الحاويات والمعرّفات المرتبة
    |--------------------------------------------------------------------------
    | لا يُستخدم لترتيب المشاهدات لأنه يتغير باستمرار.
    */
    'ranked_cache_minutes' => (int) env('DOCUMENT_SEARCH_RANKED_CACHE_MINUTES', 15),

    /*
    |--------------------------------------------------------------------------
    | تجميع وقت now() داخل مفتاح الكاش
    |--------------------------------------------------------------------------
    | يمنع published_at <= now() من إنشاء مفتاح جديد كل ثانية.
    */
    'cache_time_bucket_seconds' => (int) env('DOCUMENT_SEARCH_CACHE_TIME_BUCKET_SECONDS', 60),

    /*
    |--------------------------------------------------------------------------
    | حد أقصى لوثائق خطة التجميع المتفرقة (حماية من استعلامات ضخمة)
    |--------------------------------------------------------------------------
    */
    'scattered_plan_max_docs' => (int) env('DOCUMENT_SEARCH_SCATTERED_MAX_DOCS', 15000),

    /*
    |--------------------------------------------------------------------------
    | حد كلمات جملة البحث (حماية من استعلامات طويلة على cPanel)
    |--------------------------------------------------------------------------
    | يُطبَّق على مسار الجملة والكلمات معًا. الاستعلامات ≤ هذا العدد تبقى كما هي.
    */
    'max_query_words' => max(1, (int) env('DOCUMENT_SEARCH_MAX_QUERY_WORDS', 5)),

    /*
    |--------------------------------------------------------------------------
    | حد أحرف حقل البحث في الواجهة (maxlength)
    |--------------------------------------------------------------------------
    */
    'max_query_chars' => max(20, (int) env('DOCUMENT_SEARCH_MAX_QUERY_CHARS', 150)),

];
